export type Advantage = {
    img: string;
    description: string;
};

export type Brand = {
    id: number;
    img: string;
};

export type ProductCategory = {
    title: string;
    img: string;
    link: `/${string}`;
};

export type TopSliderComponent = {
    title: string;
    text: string;
    backgroundImage: string;
};
